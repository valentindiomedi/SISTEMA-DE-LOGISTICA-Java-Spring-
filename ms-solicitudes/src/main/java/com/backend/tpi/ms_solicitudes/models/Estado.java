package com.backend.tpi.ms_solicitudes.models;

/**
 * Estados posibles de una solicitud (enum)
 */
public enum Estado {
    PENDIENTE,
    PROGRAMADA,
    EN_TRANSITO,
    COMPLETADA,
    CANCELADA
}
